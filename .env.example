# Get your API key from https://console.anthropic.com/
# The key should start with 'sk-' for the latest Claude API
REACT_APP_CLAUDE_API_KEY=sk-ant-api03-your-key-here

# Set this to true to force using the mock API for testing without an API key
# REACT_APP_DEBUG_USE_MOCK=true